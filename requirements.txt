# This file is here so nobody has to wonder. There is nothing to install.
#
# nanoHome starts the other apps; it does not include them.
#
# Everything runs on the Python that is already on the computer, on Windows,
# macOS and Linux alike. Run it with:  python start.py
